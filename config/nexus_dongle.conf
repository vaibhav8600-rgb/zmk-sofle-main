# NEXUS dongle overrides for this keyboard.
#
# Applied on top of two files that come from the shields themselves:
#   config/boards/shields/sofle/central_dongle.conf  (BLE, split, display bus)
#   <nexus>/boards/shields/nexus_dongle/nexus_dongle.conf  (NEXUS defaults)
#
# Only what has to differ lives here. Nothing in this file edits NEXUS source,
# and nothing here affects the snake_dongle build - central_dongle.conf is
# shared, this file is not.

# --- Identity -------------------------------------------------------------
# central_dongle.conf sets "Snake". This is a different firmware, so it gets a
# different BLE name - otherwise you cannot tell which one your host paired to.
CONFIG_ZMK_KEYBOARD_NAME="NEXUS"

# --- Branding (Section 22) ------------------------------------------------
CONFIG_NEXUS_BRAND="VAIBHAV TECH"
CONFIG_NEXUS_PRODUCT="NEXUS"
CONFIG_NEXUS_SUBTITLE="SMART ZMK DONGLE"

# --- Look -----------------------------------------------------------------
# NEXUS, AMOLED, DAYLIGHT, CLAY, ESPRESSO, MINT, SUNSET
CONFIG_NEXUS_THEME="NEXUS"
CONFIG_NEXUS_DEFAULT_SCREEN_HOME=y
CONFIG_NEXUS_ANIMATIONS=y

# Panel orientation. This dongle mounts the ST7789 upside down, so the UI
# needs a half turn. Measured on hardware, not guessed.
#
# Done in software by the compositor: all drawing stays in logical top-left
# coordinates and no screen has to know (Section 8). 180 does not need the
# transpose buffer that 90/270 do - it reverses each band in place - so the
# cost here is close to nothing.
#
# The zero-cost alternative is the panel's own MADCTL byte (`mdac` in the
# shield overlay, 0xC0 for a half turn), which is deliberately NOT used here:
# a 240x240 ST7789 is a 240x320 controller showing a window, so flipping in
# hardware also needs y-offset = 80, and getting that wrong shifts the image
# instead of rotating it. Software rotation has no such trap.
CONFIG_NEXUS_DISPLAY_ROTATION=180

# --- Splash (Sections 19-21) ---------------------------------------------
CONFIG_NEXUS_SPLASH=y
CONFIG_NEXUS_SPLASH_DURATION_MS=3500

# --- Custom splash image --------------------------------------------------
# Set this and the built-in badge is not compiled at all - CMakeLists picks
# exactly one of assets/splash_default.c or the converted PNG, so there is no
# question of both appearing. Your image is then the entire splash: NEXUS
# draws no text, no wordmark and no artwork over it, because the badge's type
# lives inside the badge's own draw function and a converted PNG replaces that
# function wholesale.
#
# TO USE YOUR OWN:
#   1. save it as a PNG (not JPEG - the converter is stdlib-only, so it reads
#      PNG and nothing else) into config/nexus/splash/
#   2. uncomment the line below and point it at the file
#   3. set MAX_DIM to how large you want it; 240 fills the panel
#
# The path is relative to this config/ directory. A missing file is a
# configure-time FATAL_ERROR, not a silent fall back to the badge, so a typo
# tells you instead of quietly shipping the wrong splash.
#
# Flash cost is two bytes per pixel, uncompressed:
#   MAX_DIM 160 ->  51,200 B   (6.3% of the 792 KB app partition)
#   MAX_DIM 200 ->  80,000 B   (9.9%)
#   MAX_DIM 240 -> 115,200 B  (14.2%)
# The build prints the final size and byte count, so it is never a surprise.
#
# A photograph will band: RGB565 has 5 bits of red and blue, 6 of green, and
# there is no dithering step. Flat colour and hard edges survive it; smooth
# gradients and skin tones quantise visibly.
#
# CONFIG_NEXUS_SPLASH_IMAGE="nexus/splash/splash.png"
CONFIG_NEXUS_SPLASH_MAX_DIM=240

# --- Sound ----------------------------------------------------------------
# Same passive buzzer on the same pin the Snake build drives (P0.29).
CONFIG_NEXUS_SOUND=y
CONFIG_NEXUS_UI_SOUND=y
CONFIG_NEXUS_GAME_SOUND=y
CONFIG_NEXUS_SOUND_STARTUP=y

# Half connect/disconnect chirps, plus a slower pair for deep sleep and wake.
# Rising = arrived, falling = gone, left a fifth below right.
CONFIG_NEXUS_SOUND_SPLIT=y

# --- Games ----------------------------------------------------------------
CONFIG_NEXUS_GAME_CENTER=y
CONFIG_NEXUS_TETRIS=y
CONFIG_NEXUS_GAME_HIGHSCORE_PERSIST=y

# --- Action button --------------------------------------------------------
CONFIG_NEXUS_BUTTON=y
CONFIG_NEXUS_BUTTON_DEBOUNCE_MS=30
CONFIG_NEXUS_BUTTON_LONG_PRESS_MS=600

# --- Mouse jiggler indicator ----------------------------------------------
# The &anti_idle behavior comes from snake-module, which this config already
# pulls in for the shared keymap, so the dashboard can show whether the
# jiggler is on. Off by default in NEXUS because that module is not a NEXUS
# dependency - see Kconfig.
CONFIG_NEXUS_ANTI_IDLE_STATUS=y

# --- Status ---------------------------------------------------------------
# ZMK numbers peripherals by pairing order, not by physical side. If the
# dashboard shows the halves the wrong way round, uncomment this rather than
# re-pairing anything.
# CONFIG_NEXUS_SPLIT_SWAP_SIDES=y

# --- ZMK Studio (Sections 30-32) -----------------------------------------
# ON. The studio-rpc-usb-uart snippet in build.yaml supplies the transport,
# and config/nexus_dongle.overlay supplies the zmk,physical-layout Studio
# asserts on.
#
# Worth recording, because I got this wrong once: I removed the snippet
# claiming it broke BLE by setting CONFIG_ZMK_USB=y and making ZMK prefer USB.
# It does set that, but nice_nano_nrf52840_zmk_2_0_0_defconfig sets
# CONFIG_ZMK_USB=y for every build on this board - the Snake firmware that
# pairs perfectly included - so it was already on and was never the
# difference. USB HID is not, and never was, why BLE went quiet.
#
# Studio's actual effect on Bluetooth is the transport below, not this
# symbol - see the block immediately after.
CONFIG_ZMK_STUDIO=y

# Studio over USB only. THIS is the Studio setting that touches Bluetooth.
#
# ZMK_STUDIO_TRANSPORT_BLE defaults to y whenever ZMK_BLE is on, so enabling
# Studio silently added a GATT transport to the radio - and with it, from
# zmk/app/src/studio/Kconfig:
#
#   config BT_CONN_TX_MAX
#       default 64 if ZMK_STUDIO_TRANSPORT_BLE
#
# CONFIG_BT_CONN_TX_MAX jumping to 64 re-sizes the controller's TX buffer
# accounting on a dongle that is already carrying CONFIG_BT_MAX_CONN=7, two
# live split connections and an advertiser. It also selects
# BT_USER_DATA_LEN_UPDATE and registers another GATT service. The Snake
# firmware, which pairs on this same hardware, has none of that.
#
# Turning the BLE transport off leaves the BLE stack configured exactly as
# Snake's is, and costs nothing you use: ZMK_STUDIO_TRANSPORT_UART stays on
# (the studio-rpc-usb-uart snippet supplies the chosen zmk,studio-rpc-uart it
# keys off), so Studio still works over the USB cable - which is how it is
# used anyway, since the machine running Studio is the one the dongle is
# plugged into.
#
# If you ever want Studio over the air, set this back to y and re-test
# pairing in the same session, because that is the change to suspect.
CONFIG_ZMK_STUDIO_TRANSPORT_BLE=n

# --- Diagnostics ----------------------------------------------------------
# OFF, and it has to be: enabling it as-is does not build on this board.
#
# CONFIG_ZMK_USB_LOGGING selects UART_CONSOLE, which needs a
# `chosen zephyr,console` device. nice_nano.dts declares no console at all and
# leaves uart0 without status = "okay", so the UART log backend fails to
# compile on a symbol that was never generated:
#
#   log_backend_uart.c: error: '__device_dts_ord_DT_CHOSEN_zephyr_console_ORD'
#   undeclared
#
# TO GET LOGS, both parts are needed - the Kconfig alone is not enough. Add a
# CDC ACM console to config/nexus_dongle.overlay:
#
#   &zephyr_udc0 {
#       nexus_console: nexus_console { compatible = "zephyr,cdc-acm-uart"; };
#   };
#   / { chosen { zephyr,console = &nexus_console; }; };
#
# then set CONFIG_ZMK_USB_LOGGING=y here. The dongle enumerates a second
# serial port; open it at 115200 and reset. Take both back out afterwards -
# that extra CDC interface is not free.
CONFIG_ZMK_USB_LOGGING=n

# --- BLE link stability ---------------------------------------------------
# A dongle is not a battery keyboard, and ZMK's defaults assume it is.
#
# ZMK sets BT_PERIPHERAL_PREF_LATENCY=30 and BT_PERIPHERAL_PREF_TIMEOUT=400
# (app/Kconfig): the device is allowed to ignore 30 consecutive connection
# events, and the link is declared dead after 4 seconds of silence. That is a
# good trade on a half running off a 110 mAh cell - it is most of why those
# batteries last months.
#
# This board is plugged into the host it is talking to. It has no battery to
# protect, so it is buying nothing with that latency and paying for it in link
# fragility: skipping 30 events means the host hears from us rarely enough
# that an idle link sits close to its own inactivity threshold, and any
# hiccup takes it past the 4 s supervision timeout.
#
# Latency 0 answers every connection event. The supervision window doubles to
# 8 s. Nothing else on the dongle notices; the halves keep ZMK's defaults
# because central_dongle.conf does not set these and sofle.conf is what the
# halves read.
CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=800

# --- Display buffer -------------------------------------------------------
# central_dongle's Kconfig.defconfig defaults LV_Z_VDB_SIZE to 64, which would
# hand LVGL ~74 KB of RAM to draw nothing: NEXUS composites straight to the
# panel from a 5,760-byte static band and LVGL only holds one empty object.
# Pinned here because a .conf beats every Kconfig default regardless of the
# order the shields happen to be parsed in. (central_dongle.conf already pins
# the same value, so this is belt-and-braces rather than the only guard.)
CONFIG_LV_Z_VDB_SIZE=10

# --- Kernel heap ----------------------------------------------------------
# INHERITED, AND PROBABLY TOO BIG FOR THIS BUILD.
#
# central_dongle.conf sets CONFIG_HEAP_MEM_POOL_SIZE=65536 - 64 KB, a quarter
# of the chip's RAM. That was sized for the Snake UI, which allocated at
# runtime. NEXUS does not: every buffer it owns is static (about 6.4 KB total,
# the 5,760-byte compositor band being nearly all of it), so none of that heap
# is being used on NEXUS's behalf.
#
# It is left at the inherited value on purpose. ZMK Studio's RPC and the
# settings subsystem do allocate, and guessing their peak wrong shows up as an
# allocation failure at an awkward moment rather than at build time - which is
# exactly the trade Section 100 says not to make backwards.
#
# If you are short of RAM, this is the single biggest thing to reclaim and the
# right way to do it is empirically: uncomment, drop it a step at a time, and
# exercise Studio (connect, unlock, edit a keymap, save) plus a full Tetris
# round after each step.
#
# CONFIG_HEAP_MEM_POOL_SIZE=16384

# --- Logging (Section 65) -------------------------------------------------
CONFIG_NEXUS_DEBUG=n
